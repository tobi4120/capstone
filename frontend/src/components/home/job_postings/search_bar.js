import React from "react";

const SearchBar = () => {
    return (
        <div>
            <input type="text" placeholder="Search by title, company or any jobs keyword..." />
            <button>Find</button>
        </div>
    )
}
export default SearchBar;